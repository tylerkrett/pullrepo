{{ header }}
<div class="breadcrumb-container">
  <h1 class="page-title">{{ heading_title }}</h1>
  <ul class="breadcrumb">
    {% for breadcrumb in breadcrumbs %}
    <li class="breadcrumb-item"><a href="{{ breadcrumb.href }}">{{ breadcrumb.text }}</a></li>
    {% endfor %}
  </ul>
</div>
<div id="account-login" class="container">
  {% if success %}
    <div class="alert alert-success alert-dismissible"><i class="fa-solid fa-circle-check"></i> {{ success }} <button type="button" class="btn-close" data-bs-dismiss="alert"></button></div>
  {% endif %}
  {% if error_warning %}
    <div class="alert alert-danger alert-dismissible"><i class="fa-solid fa-circle-exclamation"></i> {{ error_warning }} <button type="button" class="btn-close" data-bs-dismiss="alert"></button></div>
  {% endif %}
  <div class="wrapper_container row">{{ column_left }}
    <div id="content" class="col">{{ content_top }}{{ content_center }}
      <div class="row">
        <div class="col-lg-6 col-sm-12">
          <div class="card mb-3">
            <div class="card-body">
              <h2>{{ text_new_customer }}</h2>
              <p><strong>{{ text_register }}</strong></p>
              <p class="mb-4">{{ text_register_account }}</p>
              <a href="{{ register }}" class="btn btn-primary">{{ button_continue }}</a></div>
          </div>
        </div>
        <div class="col-lg-6 col-sm-12">
          <div class="card mb-3">
            <div class="card-body">
              <form id="form-login" action="{{ login }}" method="post" data-oc-toggle="ajax">
                <h2>{{ text_returning_customer }}</h2>
                <p><strong>{{ text_i_am_returning_customer }}</strong></p>
                <div class="mb-3">
                  <label for="input-email" class="col-form-label">{{ entry_email }}</label>
                  <input type="text" name="email" value="{{ email }}" placeholder="{{ entry_email }}" id="input-email" class="form-control"/>
                </div>
                <div class="mb-4">
                  <label for="input-password" class="col-form-label">{{ entry_password }}</label>
                  <input type="password" name="password" value="{{ password }}" placeholder="{{ entry_password }}" id="input-password" class="form-control mb-3"/>
                  <a href="{{ forgotten }}" class="forgotten-link">{{ text_forgotten }}</a>
                </div>
                <button type="submit" class="btn btn-primary">{{ button_login }}</button>
                {% if redirect %}
                  <input type="hidden" name="redirect" value="{{ redirect }}"/>
                {% endif %}
              </form>
            </div>
          </div>
        </div>
      </div>
      {{ content_bottom }}</div>
    {{ column_right }}</div>
</div>
{{ footer }}
